# 心动花园实施方案

## 1. 目标

在现有前台网站 `you-and-me.html` 中新增一个导航项「心动花园」，用于展示 `爱心代码合集` 文件夹中的浪漫爱心代码、小动画、表白页面和相关效果图。

第一版目标是做成一个站内「浪漫代码展厅」：

- 顶部导航新增「心动花园」。
- 新增 `page-romance` 页面，风格与现有首页、相册、音乐盒保持一致。
- 页面使用卡片墙展示不同小项目。
- 点击可直接预览的 HTML 项目时，在站内弹窗中通过 `iframe` 展示。
- 第一阶段只展示 HTML 项目；Python、TXT、zip 等暂不接入。

## 2. 当前项目判断

前台核心页面是根目录的 `you-and-me.html`，这是一个单文件静态前台，包含：

- 固定顶部导航：`首页 / 纪念日 / 相册 / 音乐盒 / 设置`
- 页面切换函数：`showPage(id, btn)`
- 页面容器格式：`<div class="page" id="page-home">`
- 统一视觉风格：深红背景、玫瑰色主色、玻璃卡片、柔光动效、左侧资料卡 + 右侧内容区
- 后台数据接口：`/api/public/spaces/default/bootstrap`

`爱心代码合集` 中存在多种资源：

- 可直接预览的 HTML 项目，例如：
  - `爱心代码合集/蓝色粒子爱心/蓝色粒子爱心1.html`
  - `爱心代码合集/爱心树/index.html`
  - `爱心代码合集/lovered-红色爱心变幻钻石/index.html`
  - `爱心代码合集/3d照片墙/3d照片墙.html`
  - `爱心代码合集/爱心流星雨背景_超好看/爱心流星雨背景_超好看/love.html`
  - `爱心代码合集/送给最爱的你.html`
- Python/TXT/zip 暂不进入心动花园展示，只保留在 `爱心代码合集` 中等待后续整理。
- 图片效果图，可作为不能直接运行项目的预览图。

## 3. 页面信息架构

### 3.1 导航

在现有导航中添加：

```html
<button class="nav-link" onclick="showPage('romance',this)">
  <span class="nav-icon">🌹</span> 心动花园
</button>
```

建议放在「音乐盒」后、「设置」前。

### 3.2 页面布局

新增页面：

```html
<div class="page" id="page-romance">
  <div class="content-grid">
    <div class="sidebar">...</div>
    <div class="main-content">...</div>
  </div>
</div>
```

页面结构：

- 左侧：复用个人资料卡和心动指数卡。
- 右侧顶部：页面头图卡片。
- 右侧主体：筛选栏 + 项目卡片网格。
- 弹窗：用于站内预览 HTML 项目。

### 3.3 页面文案

页面标题：

```text
心动花园 🌹
```

页面副标题：

```text
把收集来的爱心代码，变成可以随手打开的小惊喜
```

空状态文案：

```text
还没有整理好的浪漫项目。
```

## 4. 卡片设计

每个小项目卡片包含：

- 封面区域：图片、渐变预览或 iframe 缩略占位。
- 标题：项目名称。
- 类型标签：`HTML 可预览`、`待上传 HTML`。
- 简短描述：一句话说明这个项目的浪漫点。
- 操作按钮：
  - HTML 项目：`打开浪漫`
  - 暂未上传 HTML 项目：`待上传`

推荐卡片字段：

```js
{
  id: 'blue-particle-heart',
  title: '蓝色粒子爱心',
  type: 'html',
  tag: '粒子爱心',
  description: '蓝色光点慢慢聚成一颗心，适合做安静的小惊喜。',
  url: '/heart-garden/蓝色粒子爱心/蓝色粒子爱心1.html',
  cover: '',
  status: 'ready'
}
```

## 5. 第一版项目清单

第一版优先接入这些可直接 iframe 预览的 HTML 项目：

```js
const romanceProjects = [
  {
    id: 'blue-particle-heart',
    title: '蓝色粒子爱心',
    type: 'html',
    tag: '粒子',
    description: '蓝色粒子聚成爱心，克制又漂亮。',
    url: '/heart-garden/蓝色粒子爱心/蓝色粒子爱心1.html',
    status: 'ready'
  },
  {
    id: 'love-tree',
    title: '爱心树',
    type: 'html',
    tag: '告白',
    description: '一棵慢慢生长出来的爱心树，适合纪念日打开。',
    url: '/heart-garden/爱心树/index.html',
    status: 'ready'
  },
  {
    id: 'red-diamond-heart',
    title: '红色爱心变幻钻石',
    type: 'html',
    tag: '3D',
    description: '红色心形与钻石质感变幻，视觉冲击更强。',
    url: '/heart-garden/lovered-红色爱心变幻钻石/index.html',
    status: 'ready'
  },
  {
    id: 'photo-wall-3d',
    title: '3D 照片墙',
    type: 'html',
    tag: '照片',
    description: '把回忆做成环绕式照片墙。',
    url: '/heart-garden/3d照片墙/3d照片墙.html',
    status: 'ready'
  },
  {
    id: 'meteor-love',
    title: '爱心流星雨',
    type: 'html',
    tag: '背景',
    description: '夜色里落下爱心流星，适合做氛围背景。',
    url: '/heart-garden/爱心流星雨背景_超好看/爱心流星雨背景_超好看/love.html',
    status: 'ready'
  },
  {
    id: 'for-my-love',
    title: '送给最爱的你',
    type: 'html',
    tag: '表白',
    description: '完整表白页面，适合作为小惊喜入口。',
    url: '/heart-garden/送给最爱的你.html',
    status: 'ready'
  },
];
```

## 6. 交互方案

### 6.1 筛选

页面顶部提供轻量筛选按钮：

- 全部
- 可预览
- 粒子爱心
- 告白页面
- 自定义

点击筛选时只重新渲染卡片，不跳转页面。

### 6.2 预览弹窗

HTML 项目点击后打开弹窗：

```html
<div class="romance-preview-modal" id="romancePreviewModal">
  <button class="romance-preview-close">×</button>
  <div class="romance-preview-frame-wrap">
    <iframe id="romancePreviewFrame"></iframe>
  </div>
  <div class="romance-preview-footer">
    <div>
      <div id="romancePreviewTitle"></div>
      <div id="romancePreviewDesc"></div>
    </div>
    <a id="romancePreviewOpen" target="_blank">全屏打开</a>
  </div>
</div>
```

交互规则：

- 点击「打开浪漫」：设置 iframe 的 `src`，显示弹窗。
- 点击关闭：清空 iframe `src`，避免音频或动画继续运行。
- 点击「全屏打开」：新标签页打开原始 HTML。

### 6.3 非 HTML 项目

Python/TXT/zip 项目第一阶段不展示、不执行。后续如果需要，可以把效果改写成 HTML/Canvas 版本后再加入心动花园。

## 7. 样式方案

新增样式应沿用现有 CSS 变量：

- `--bg-card`
- `--bg-card2`
- `--rose`
- `--rose-light`
- `--gold`
- `--text-primary`
- `--text-secondary`
- `--text-muted`
- `--border`
- `--glass`

新增类建议：

```css
.romance-toolbar {}
.romance-filter {}
.romance-grid {}
.romance-card {}
.romance-cover {}
.romance-tag {}
.romance-card-title {}
.romance-card-desc {}
.romance-actions {}
.romance-preview-modal {}
.romance-preview-frame-wrap {}
```

卡片视觉：

- 背景使用 `var(--bg-card)`。
- 边框使用 `var(--border)`。
- 圆角保持 18px 左右，贴合现有玻璃卡片。
- hover 时轻微上浮，边框变亮，出现玫瑰色柔光。
- 不使用完全跳脱的颜色，避免破坏当前网站氛围。

## 8. 具体实现步骤

### 步骤 1：新增导航

修改 `you-and-me.html` 的 `<nav>`：

- 在「音乐盒」后新增「心动花园」按钮。
- 保持 `nav-link` 类名。
- 使用 `showPage('romance', this)`。

注意：现有 `goToPageFromBackend('album', 2)` 和 `goToPageFromBackend('music', 3)` 依赖导航索引。因为新增项放在音乐后，不会影响相册和音乐索引。

### 步骤 2：新增页面 HTML

在 `page-music` 和 `page-settings` 之间插入 `page-romance`：

- 复用侧栏结构。
- 新增页面头图。
- 新增筛选栏。
- 新增 `.romance-grid` 容器。

### 步骤 3：新增预览弹窗 HTML

在现有 `mediaLightbox` 附近新增 `romancePreviewModal`。

要求：

- 弹窗层级高于普通页面，低于重要 toast 即可。
- iframe 区域自适应桌面和手机。
- 关闭时清空 iframe。

### 步骤 4：新增 CSS

在 `you-and-me.html` 的 `<style>` 中新增心动花园相关样式。

重点覆盖：

- 卡片网格桌面端 3 列或 4 列。
- 平板端 2 列。
- 手机端 1 列。
- iframe 弹窗在手机上高度不超过视口，底部信息不遮挡内容。

### 步骤 5：新增 JS 数据和渲染函数

在脚本区新增：

```js
const romanceProjects = [...];
let romanceFilter = '全部';

function renderRomanceProjects(filter = romanceFilter) {}
function openRomanceProject(id) {}
function closeRomancePreview() {}
function bindRomanceFilters() {}
```

渲染逻辑：

- 根据 `romanceFilter` 过滤。
- HTML 项目按钮显示 `打开浪漫`。
- 未上传 HTML 的项目按钮显示 `待上传`。
- 使用已有 `escapeHtml()` 做文本转义。

### 步骤 6：接入整体渲染

在 `renderSpace(data)` 中加入：

```js
renderRomanceProjects();
bindRomanceFilters();
```

如果后端未启动，静态页面仍应可展示心动花园内容。因此还需要在脚本初始化阶段调用一次：

```js
renderRomanceProjects();
bindRomanceFilters();
```

### 步骤 7：处理移动端导航

当前顶部导航按钮较多，新增「心动花园」后手机端可能拥挤。

第一版可做：

- `.nav-links` 在小屏横向滚动。
- `nav` 保持固定高度。
- 不让按钮文字换行。

后续如果导航继续增加，再考虑折叠菜单。

## 9. 验收标准

完成后应满足：

- 顶部导航出现「心动花园」。
- 点击「心动花园」能切换到新页面。
- 页面视觉与现有网站一致，不像外来的独立页面。
- 至少 6 个 HTML 项目卡片可点击预览。
- 预览弹窗可以关闭，关闭后 iframe 停止加载。
- 「全屏打开」能在新标签页打开原始项目。
- 后端未启动时，心动花园页面仍能正常展示。
- 桌面端、平板端、手机端卡片布局不重叠。
- 新增代码不影响首页、纪念日、相册、音乐盒、设置页的原有功能。

## 10. 后续增强

第一版完成后，可以继续升级：

- 在后台管理中增加「心动花园」管理入口。
- 将项目清单从静态 JS 迁移到数据库。
- 自动扫描 `爱心代码合集` 中的 HTML 文件生成项目列表。
- 为每个项目生成封面截图。
- 把部分 Python 爱心代码改写成浏览器 Canvas 版本。
- 给「心动花园」增加收藏、最近打开、随机惊喜功能。
