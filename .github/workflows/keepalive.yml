name: Keep Render Warm

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API health
        run: curl --fail --silent --show-error --max-time 20 https://api.likeu.love/api/health
